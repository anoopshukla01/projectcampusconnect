import StudentSelfView from '../StudentDetail/StudentDetail';

/**
 * Student Profile & Settings Page
 * Renders the full student profile details, self-editing controls, and Skills section.
 */
export default function Settings() {
  return <StudentSelfView />;
}
