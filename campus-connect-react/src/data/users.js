/**
 * Offline fallback user list — intentionally empty.
 *
 * All authentication flows through the real backend (/api/v1/auth/login).
 * DEV-mode offline login is disabled; mock accounts have been permanently removed.
 */
export const USERS = [];


