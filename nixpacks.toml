[phases.setup]
nixPkgs = ["python313", "postgresql"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "cd backend && gunicorn \"app:create_app('production')\" --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
